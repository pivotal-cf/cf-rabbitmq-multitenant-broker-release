<%
    rabbitmq_broker_config = p("rabbitmq-service-broker")

    if_link("rabbitmq-haproxy") do |haproxy|
        rabbitmq_broker_config["rabbitmq"]["hosts"] = haproxy.instances.map(&:address)
    end
%>

<%= rabbitmq_broker_config.to_yaml %>
