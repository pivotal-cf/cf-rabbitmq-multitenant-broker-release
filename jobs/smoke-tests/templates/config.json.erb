<%
  require 'json'

  conf = {
      api: p("cf.api_url"),
      admin_user: p("cf.admin_username"),
      admin_password: p("cf.admin_password"),
      skip_ssl_validation: true,
      org_name:  p('cf.org'),
      space_name: p("cf.space"),
      service_offering: p("cf.service_offering_name"),
      app_type: "fifo",
      plans: [{name: "standard"}]
  }
%>

<%= JSON.dump(conf) %>
